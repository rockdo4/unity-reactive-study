# 03. 연산자 이론 — 강사용

> 🎙️ 03차시 **이론 수업(~40분) 진행 가이드**입니다. 절마다 핵심 멘트·판서·보여줄 코드를 담아 **이 문서만으로 수업을 진행**할 수 있습니다. 학생 화면에는 노션 교안(교안 03)을 공유합니다.
>
> - **후속 수업**: [03. 연산자 따라하기 - 강사용](03.%20연산자%20따라하기%20-%20강사용.md)
> - **학생 교안**: [03. 연산자 - 스트림을 다듬고 합성하기](../교안/03.%20연산자%20-%20스트림을%20다듬고%20합성하기.md)
> - 📂 도구: [씬 생성·패키지 도구 가이드](../데모/씬%20생성·패키지%20도구%20가이드.md)

## 📚 수업 개요

- **시간**: 약 40분 (도입 3분 + 5개 절)
- **난이도**: 초급~중급 · **선수 지식**: 02차시(Observable 만들기·Subscribe), LINQ 기본
- **학습 목표**
    - 연산자 = Observable → Observable 함수이며, 체인으로 파이프라인을 만들고 **구독은 맨 끝 한 번**임을 이해한다
    - 필터·변환(`Where`/`Select`/`DistinctUntilChanged`/`Scan`)을 LINQ 지식으로 읽는다
    - 시간 3총사(`Debounce`/`ThrottleFirst`/`Chunk`)의 발화 시점을 마블로 구분한다
    - 결합 2총사(`Merge`/`CombineLatest`)로 "여러 입력원 → 한 행동", "여러 값 → 한 조건"을 표현한다
    - UniRx → R3 이름 매핑(`Throttle→Debounce`, `Buffer→Chunk`)을 즉답한다

## ✅ 수업 전 체크리스트

- [ ] **이 차시가 강의의 핵심** — "연산자 합성으로 의도를 그대로 코드에 적는다"를 체감하게 하는 시간. 1차시 명령형 더블클릭(코루틴+플래그+타이머)이 두 줄이 되는 장면을 핵심 장면으로 설계할 것.
- [ ] **전부 가르치려 하지 말 것** — 연산자는 수십 개고 카탈로그는 사전임. 수업에서 제대로 다루는 건 8개뿐: `Where` `Select` `DistinctUntilChanged` `Scan` / `Debounce` `ThrottleFirst` `Chunk` / `Merge` `CombineLatest`. 나머지는 "그룹에서 찾아 쓰는 법"만.
- [ ] **Debounce vs ThrottleFirst를 자기 입으로** — 수업 전 소리 내어 연습: "Debounce는 **끝나면**(잠잠해진 뒤 마지막), ThrottleFirst는 **먼저**(첫 발 통과 후 구간 무시)." 연타 입력 하나를 놓고 두 연산자가 어디서 발화하는지 마블로 그릴 수 있어야 함.
- [ ] **UniRx 매핑표 숙지** — 학생이 검색해 오는 코드가 UniRx일 확률이 높음. `Throttle→Debounce`(최대 함정), `Buffer→Chunk`, `.Count→.Length` 세 개는 즉답할 수 있어야 함.
- [ ] 노션 교안 03 화면 공유 준비

## 🕐 타임테이블

| 절  | 주제                                       | 시간 |
| --- | ------------------------------------------ | ---- |
| 0   | 도입 — 명령형 더블클릭 다시 보기           | 3분  |
| 3-1 | 파이프라인과 카탈로그                      | 7분  |
| 3-2 | 필터·변환 — Where·Select·Distinct·Scan     | 7분  |
| 3-3 | 시간 연산자 — Debounce·ThrottleFirst·Chunk | 10분 |
| 3-4 | 결합 연산자 — Merge·CombineLatest          | 5분  |
| 3-5 | Share와 UniRx 이름 매핑                    | 8분  |

---

## 0. 도입 (3분)

> 💡 1차시의 명령형 더블클릭 코드를 다시 띄우고 — "오늘 이게 두 줄이 됩니다. 그리고 왜 두 줄이 '짧아서'가 아니라 '의도가 보여서' 좋은지 이야기합니다."

**핵심 메시지**: 스트림을 거르고(`Where`), 바꾸고(`Select`), 시간으로 다듬고(`Debounce`/`ThrottleFirst`/`Chunk`), 여러 스트림을 합쳐(`Merge`/`CombineLatest`) **의도를 그대로 코드로 적는** 법을 배운다.

---

## 3-1. 파이프라인과 카탈로그 (7분)

**핵심 메시지**

- 연산자(operator)는 **Observable을 받아 새 Observable을 돌려주는 메서드**. 그래서 메서드 체인으로 파이프라인을 만들 수 있다.
- 체인 = 파이프라인, **구독은 맨 끝 한 번**. 중간 단계는 전부 "설계도"고, `Subscribe`가 호출돼야 물이 흐른다.

**판서** — `source → Where → Select → Subscribe` 관로 그림:

```csharp
source                      // 원본 스트림
    .Where(x => x > 0)      // 거르고
    .Select(x => x * 10)    // 바꾸고
    .Subscribe(Print);      // 마지막에 구독
```

```
source: --(-1)--2--0--3-->
Where:  --------2-----3-->     (0보다 큰 것만)
Select: --------20----30-->    (×10)
```

**카탈로그 — 분류만 훑기**: "필요할 때 그룹에서 찾는다"는 태도를 심는다. 표는 외우는 게 아니라 분류만.

| 그룹          | 대표 연산자 (R3 기준)                                             | 하는 일                      |
| ------------- | ----------------------------------------------------------------- | ---------------------------- |
| **생성**      | `Range` · `Timer` · `Interval` · `FromEvent` · `EveryUpdate`      | 스트림을 만듦 (2차시)        |
| **필터**      | `Where` · `Distinct` · `DistinctUntilChanged` · `Take` · `Skip`   | 조건·개수로 걸러냄           |
| **변환**      | `Select` · `Cast` · `Index`                                       | 값을 다른 모양으로           |
| **시간**      | `Debounce` · `ThrottleFirst` · `ThrottleLast` · `Delay` · `Chunk` | 시간으로 다듬음              |
| **결합**      | `Merge` · `CombineLatest` · `Zip` · `WithLatestFrom` · `Concat`   | 여러 스트림을 합침           |
| **누적**      | `Scan` · `Aggregate`                                              | 흘러온 값을 누적함           |
| **에러/완료** | `OnErrorResume` 처리 · `Catch` · `OnErrorResumeAsFailure`         | 에러·완료를 다룸 (5차시)     |
| **공유**      | `Share` · `Replay` 계열                                           | 하나의 구독을 여럿이 나눠 씀 |

---

## 3-2. 필터와 변환 — Where · Select · DistinctUntilChanged · Scan (7분)

**핵심 메시지**: LINQ와 같은 이름, 같은 의미. 대상이 "시간 위의 스트림"일 뿐. `Where`/`Select`는 LINQ 그대로라 빠르게 넘긴다.

**보여줄 코드**:

```csharp
// 10점 단위 점수만 통과시켜 마일스톤 알림
playerStat.OnScoreChanged
    .Where(score => score % 10 == 0)
    .Subscribe(score => Debug.Log($"마일스톤! {score}"));

// HP를 "구간 라벨"로 변환하고, 구간이 바뀔 때만 갱신
vitals.Hp
    .Select(hp => hp < 30 ? "위험" : hp < 70 ? "주의" : "안전")
    .DistinctUntilChanged()                 // 같은 라벨이 연속되면 무시
    .Subscribe(state => m_StateText.text = state);
```

- `DistinctUntilChanged`는 **연속 중복 제거** — "바뀔 때만 한 번"(UI 갱신·사운드 재생) 반응해야 하는 곳의 단골.

**Scan** — "지금까지의 누적"을 흘려보냄(LINQ `Aggregate`의 실시간 버전). 마블로 보여 줄 것:

```csharp
// 획득 골드 스트림 → 누적 합계 스트림
goldGained.Scan(0L, (total, gained) => total + gained)
    .Subscribe(total => m_TotalText.text = $"누적 {total}");
```

```
in:   --5---3---10-->
Scan: --5---8---18-->    (seed 0에서 시작해 누적)
```

> 💡 "2부 클리커의 콤보가 이걸로 만들어진다"고 예고.

---

## 3-3. 시간 연산자 — Debounce · ThrottleFirst · Chunk (10분, 핵심)

**핵심 메시지**: 시간 연산자는 이 차시의 핵심. **상태 플래그와 타이머를 직접 들지 않고** 시간 조건을 선언한다. Debounce(검색창)·ThrottleFirst(쿨다운) 마블을 칠판에 크게 그리고, **같은 입력 스트림**에 두 연산자를 적용한 출력 차이를 학생에게 그리게 한다.

### Debounce — 잠잠해질 때까지 기다리기

마지막 이벤트 후 지정 시간 동안 새 이벤트가 없으면, 그 마지막 값을 내보낸다.

```
in:   --a-b--c----------d----->
Debounce(0.5s):
out:  ------------c----------d-->   (연타 중에는 침묵, 잠잠해지면 마지막 값)
```

```csharp
// 검색창: 타이핑이 0.5초 멈췄을 때만, 직전과 다른 검색어일 때만 검색 실행
m_SearchInput.onValueChanged.AsObservable()
    .Debounce(TimeSpan.FromMilliseconds(500))
    .DistinctUntilChanged()
    .Subscribe(query => Search(query));
```

쓰임: 검색 자동완성, 설정 자동 저장, 리사이즈 후 재배치 — "**입력이 끝났을 때** 한 번".

### ThrottleFirst — 첫 발만 통과, 구간 무시

첫 이벤트를 통과시키고, 지정 시간 동안 나머지를 무시한다.

```
in:   --a-b-c----d-e-------f-->
ThrottleFirst(1s):
out:  --a--------d---------f-->   (발동 직후 1초는 귀를 닫음)
```

```csharp
// 스킬 버튼: 첫 클릭만 발동, 2초 쿨다운
m_SkillButton.OnClickAsObservable()
    .ThrottleFirst(TimeSpan.FromSeconds(2))
    .Subscribe(_ => CastSkill());
```

쓰임: 스킬 쿨다운, 연타 방지, 공격 입력 제한 — "**먼저 발동하고** 잠시 무시".

> 💡 **Debounce는 "끝나면", ThrottleFirst는 "먼저"**. 같은 연타 입력에 Debounce는 마지막에 한 번, ThrottleFirst는 처음에 한 번 반응한다.

### Chunk — 모아서 묶기

이벤트를 모았다가 **경계**(시간·개수·다른 스트림)가 오면 배열로 내보낸다.

```csharp
// 500ms 안에 3번 클릭 → 3연타
taps.Chunk(TimeSpan.FromMilliseconds(500), 3)
    .Where(xs => xs.Length >= 3)
    .Subscribe(_ => Debug.Log("3연타!"));

// 더블클릭: "클릭이 250ms 잠잠해질 때"를 경계로 클릭을 묶고, 2개 이상이면
var clicks = Observable.EveryUpdate()
    .Where(_ => Mouse.current != null && Mouse.current.leftButton.wasPressedThisFrame)
    .Share();                                            // ← 3-5에서 설명

clicks
    .Chunk(clicks.Debounce(TimeSpan.FromMilliseconds(250)))   // 경계 = 잠잠해진 순간
    .Where(xs => xs.Length >= 2)
    .Subscribe(_ => Debug.Log("더블 클릭"));
```

- 더블클릭 파이프라인 완성 후 **"한 문장으로 읽어 보세요"**(답: "클릭이 잠잠해질 때까지 모아서, 2개 이상이면"). 1차시의 명령형 더블클릭(코루틴+플래그+타이머)과 비교 — **의도가 그대로 코드**가 된다.

**칠판 정리**: `Debounce = 끝나면 · ThrottleFirst = 먼저 · Chunk = 모아서`.

> 🔁 **Unity에서 늘 하던 방식 ↔ R3 (시간·입력 다루기)** — `Update`와 코루틴으로 손수 짜던 입력·시간 로직이 시간 연산자로 무엇이 되는지의 지도.
>
> | Unity에서 늘 하던 방식                          | 무엇이 문제였나                           | R3 대응                                                      |
> | ----------------------------------------------- | ----------------------------------------- | ------------------------------------------------------------ |
> | `Update`에서 매 프레임 입력 폴링 + 조건 분기    | 안 바뀐 프레임도 매번 검사, 조건이 흩어짐 | `EveryUpdate()` + `Where` — 조건을 스트림 한 곳에 선언       |
> | 코루틴+플래그+타이머로 더블클릭·연타 판정       | 상태 플래그·타이머 누적 → 경계·리셋 버그  | `Chunk` + `Debounce`(경계) — "모아서 N개 이상"이 그대로 코드 |
> | `if (Time.time - last > cd)`로 쿨다운·연타 방지 | 시각 비교 변수 수동 관리, 재사용 어려움   | `ThrottleFirst` — "먼저 한 번, 구간 무시"를 한 줄로          |
>
> 메타 축 둘만 기억: **폴링(pull, 끌어오기) → 스트림 구독(push, 밀어주기)**, **명령형 how(플래그·타이머) → 선언형 what(연산자 합성)**.

---

## 3-4. 결합 연산자 — Merge · CombineLatest (5분)

### Merge — 여러 스트림을 한 줄로

```
A:      --a-----c----->
B:      -----b-----d-->
Merge:  --a--b--c--d-->    (도착 순서대로 한 스트림에)
```

```csharp
// 키보드 스페이스든 버튼 클릭이든 "발사"는 발사
var fireByKey = Observable.EveryUpdate()
    .Where(_ => Keyboard.current != null && Keyboard.current.spaceKey.wasPressedThisFrame)
    .Select(_ => Unit.Default);
var fireByButton = m_FireButton.OnClickAsObservable();

fireByKey.Merge(fireByButton)
    .ThrottleFirst(TimeSpan.FromSeconds(0.5))   // 합친 뒤 공통 쿨다운 적용
    .Subscribe(_ => Fire());
```

→ Merge는 "여러 입력원 → 한 행동".

### CombineLatest — 각 스트림의 "최신값" 조합

어느 쪽이든 새 값이 오면, **모든 스트림의 최신값**을 모아 내보낸다.

```
HP:              --100--------70------------------30------->
MP:              --50-------------------10----------------->
CombineLatest:   --(100,50)---(70,50)---(70,10)---(30,10)-->
```

```csharp
// HP가 있고 MP가 충분할 때만 스킬 버튼 활성화 — "조건이 여러 값에 걸칠 때"의 정석
hp.CombineLatest(mp, (h, m) => h > 0 && m >= 20)
    .DistinctUntilChanged()
    .Subscribe(canUse => m_SkillButton.interactable = canUse);
```

> 💡 CombineLatest는 "버튼 활성화의 정석"으로 **5차시·2부에서 반복됨**을 예고. 대부분의 게임 UI는 `CombineLatest`로 충분하다. `Zip`(짝이 맞을 때만)·`WithLatestFrom`(한쪽이 주도)은 이름만 — 카탈로그 행.

---

## 3-5. Share와 UniRx 이름 매핑 (8분)

### Share — 핫/콜드 혼동 방지

더블클릭 코드에서 `clicks`를 **본체와 Debounce 경계 두 곳**이 쓴다. `Share()`가 없으면 두 곳이 **각자 따로 구독**해서 `EveryUpdate` 검사가 두 벌 돌게 된다(콜드하게 동작). 값비싼 스트림(네트워크·무거운 계산)이라면 이게 버그가 된다.

```csharp
var clicks = rawClicks.Share();   // 원본을 한 번만 구독하고, 구독자들이 그 결과를 나눠 받음
```

규칙: 한 스트림을 두 곳 이상에서 구독하면, 콜드 재실행(중복 구독)이 일어나는지 의심하고 `Share()`로 묶을지 검토할 것.

> ⚠️ **함정**: `Share()` 질문이 나오면 — "clicks를 본체와 Debounce 경계 두 곳이 써서, 구독을 하나로 공유하는 것"까지만. 깊은 핫/콜드 강의로 빠지지 말 것(콜드/핫 이론은 02차시 수준으로 제한).

### 옛 UniRx 코드를 읽을 때 (이름 매핑) — 강사 배경지식 / 따라하기 고의 실패 시연용

> 📌 이 매핑표는 **교안에서는 제외**됐다(노션 교안 03에 없음). 강사 배경지식이자 따라하기 "고의 실패 시연"의 근거로만 쓴다. 학생 화면(교안)에는 띄우지 말고, 필요할 때 강사가 구두로 풀어 준다.

인터넷 튜토리얼의 다수가 UniRx 기준이다. R3는 연산자 이름을 .NET LINQ 표준에 맞춰 바꿨으므로 **그대로 붙이면 컴파일이 안 된다.** (따라하기에서 이걸 **고의 실패 시연**으로 각인시킨다.)

| UniRx                           | R3                            | 함정/메모                                                             |
| ------------------------------- | ----------------------------- | --------------------------------------------------------------------- |
| `Buffer`                        | `Chunk`                       | 결과가 `IList<T>`(`.Count`) → **`T[]`(`.Length`)** — 개수 검사도 바뀜 |
| `Throttle`                      | `Debounce`                    | **최대 함정** — R3엔 `Throttle`이 아예 없음                           |
| `Sample`                        | `ThrottleLast`                |                                                                       |
| `StartWith`                     | `Prepend`                     |                                                                       |
| `ThrottleFirst`                 | `ThrottleFirst`               | 이름·동작 그대로                                                      |
| `IObservable<T>`/`IObserver<T>` | `Observable<T>`/`Observer<T>` | 인터페이스 → 추상 클래스                                              |
| `this.UpdateAsObservable()`     | `Observable.EveryUpdate()`    | R3 전역 스트림은 수명 관리 필수(4차시)                                |

핵심 프레임: **개념은 그대로, 이름이 LINQ 표준에 맞춰 바뀌었다.**

---

# 🎓 이론 마무리 (3분)

| 개념       | 한 줄 요약                                                               |
| ---------- | ------------------------------------------------------------------------ |
| 연산자     | Observable → Observable 함수. 체인으로 파이프라인, 구독은 맨 끝 한 번    |
| 분류       | 생성·필터·변환·시간·결합·누적·에러·공유 — 외우지 말고 분류만             |
| 시간 3총사 | **Debounce("끝나면")** · **ThrottleFirst("먼저")** · **Chunk("모아서")** |
| 결합 2총사 | **Merge(한 줄로)** · **CombineLatest(최신값 조합 → 버튼 활성화의 정석)** |
| Share      | 한 스트림을 두 곳 이상에서 쓰면 의심                                     |
| UniRx → R3 | `Buffer→Chunk`(`.Count→.Length`), `Throttle→Debounce`                    |

"쉬는 시간 후, 방금 본 연산자들을 전부 Unity 씬(`ReactiveBasics` 씬 §2 연산자 섹션)에서 직접 조작해 확인합니다." → [따라하기 수업](03.%20연산자%20따라하기%20-%20강사용.md)

---

## 💡 강사 팁

- 3-3 시간 연산자가 이 수업의 핵심 — 같은 연타 입력에 Debounce/ThrottleFirst가 어디서 발화하는지 학생이 직접 마블로 그려 보게 하면 가장 잘 남는다.
- 카탈로그(3-1 표)는 "다 외우려 들지 말 것"을 거듭 강조 — 분류 감각만 잡으면 충분.
- 시간이 늘어지면 `Merge`(3-4 앞부분)는 멘트로 끝내고 따라하기에서 다뤄도 무방. 단 `CombineLatest`는 5차시·2부의 복선이므로 반드시 짚는다.

## 부록 — 예상 Q&A

> ❓ **연산자를 다 외워야 하나요?**
> 아님. 분류(생성/필터/변환/시간/결합/누적)만 잡고 필요할 때 공식 문서에서 찾는 것. 실무에서 자주 쓰는 건 오늘 배운 8개 안팎.

> ❓ **Debounce랑 ThrottleLast는 뭐가 다른가요?**
> ThrottleLast(=UniRx Sample)는 **고정 주기마다** 구간의 마지막 값을 내보냄(잠잠함과 무관). Debounce는 **잠잠해져야** 내보냄. 입력이 계속되는 동안 ThrottleLast는 주기적으로 나오고 Debounce는 침묵.

> ❓ **체인이 길어지면 디버깅은 어떻게 하나요?**
> ①중간 스트림을 지역 변수로 쪼개 이름을 붙인다 ②`Do`(R3의 사이드 이펙트 연산자)나 중간 `Subscribe`+로그로 단계별 값을 찍는다 ③Tracker로 구독 자체를 확인(4차시). "7~8개 이상 이어지면 설계를 의심"이 교안의 권고.

> ❓ **`InvokeRepeating`으로 주기 반복하던 건 R3로 어떻게 바뀌나요?** (전환형)
> `Observable.Interval(TimeSpan.FromSeconds(N))`이 정확한 대응 — 차이는 반환이 스트림이라 뒤에 `Where`·`Debounce`·`ThrottleFirst`를 바로 이어 "주기 반복 + 시간 가공"을 한 체인으로 표현할 수 있다는 것. 구독은 `AddTo`로 수명 결합. ('기존 방식 → R3' 한눈 매핑표는 [00. 강사 사전 학습 가이드](00.%20강사%20사전%20학습%20가이드.md))

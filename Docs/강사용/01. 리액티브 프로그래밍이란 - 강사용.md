# 01. 리액티브 프로그래밍이란 — 강사용

> 진행 메모. 1부 첫 차시 이론 수업(~50분). 코드보다 프레임 전달이 목적. 학생 화면=노션 교안 01 공유.
>
> - **학생 교안**: [01. 리액티브 프로그래밍이란](../교안/01.%20리액티브%20프로그래밍이란.md)
> - **운영 가이드**: [00. 수업 개요](../00.%20수업%20개요.md)
> - 데모·따라하기 없음(순수 이론). R3 코드는 02차시부터 데모 씬으로 직접 다룸.

## 📚 수업 개요

- **시간**: 약 50분 (도입 7분 + 7개 절 + 마무리 2분)
- **난이도**: 초급 · **선수 지식**: C# `event`·델리게이트·람다, `MonoBehaviour` 생명주기
- **학습 목표**
    - 리액티브 = 옵저버 패턴의 일반화/확장임을 설명한다
    - 이벤트를 "시간 위를 흐르는 데이터 스트림"으로 보는 발상 전환을 이해한다
    - 마블 다이어그램(시간 축·값·완료·에러)을 읽는다
    - push/pull 방향 전환(밀기/끌기)과 R3 계보를 구분한다

## ✅ 수업 전 체크리스트

- [ ] 두 문장 자기 말로 정리 — ① "리액티브는 **옵저버 패턴의 일반화/확장**"(ReactiveX 공식 정의) ② "이벤트를 **시간 위를 흐르는 데이터 스트림**으로 본다"(발상 전환의 전부)
- [ ] 마블 손그림 연습 — `Where`/`Select`/`Debounce` 세 개. 가로선(시간)·구슬(값)·`|`(완료)·`X`(에러)
- [ ] 역사 파트 가드레일 — [00. 강사 사전 학습 가이드](00.%20강사%20사전%20학습%20가이드.md) 준수. 날짜·버전·점유율 단정 금지
- [ ] 노션 교안 01 화면 공유 준비

## 🕐 타임테이블

| 절  | 주제                                  | 시간 |
| --- | ------------------------------------- | ---- |
| 0   | 도입 — 왜 배우는가                    | 7분  |
| 1   | 게임 코드는 "반응"으로 가득하다       | 10분 |
| 2   | 토대: 옵저버 패턴                     | 8분  |
| 3   | 리액티브 = 옵저버 패턴의 확장         | 10분 |
| 4   | 스트림으로 생각하기 — 마블 다이어그램 | 8분  |
| 5   | push vs pull                          | 5분  |
| 6~7 | 선언적 합성 미리 보기 + Rx 계보       | 5분  |

> ℹ️ **강사 사전 학습 · 리마인드**
>
> - **옵저버 패턴 먼저** — 디자인 패턴 수업 후속 아님. 모르는 학생 있을 수 있으니 2절에서 주체/관찰자/`event` 자족적으로 다룸. 아는 학생=이름표 바꾸기, 모르는 학생=미니 옵저버 수업.
> - **마블은 강사가 손으로 그릴 것** — 4절은 칠판 중심. 사전 연습 필수.
> - **push/pull 방향 전환** — `IEnumerable`(소비자가 `MoveNext`로 끌어옴) vs `Observable`(생산자가 `OnNext`로 밀어 넣음). LINQ 아는 학생엔 "같은 연산자를 시간 축에 적용"이 지름길.

---

## 0. 도입 (7분)

- 폴링(`Update`)·이벤트 양쪽 다 한계 보여 주고 시작. 폴링=안 바뀐 99% 프레임도 검사, 이벤트=`OnDestroy`에서 `-=` 누락.
- 학습 목표 4개 제시 — ① 리액티브=옵저버의 확장 ② 마블 읽기 ③ push/pull 구분 ④ R3 계보.

> 💡 이 차시 코드는 전부 **읽기만**. 직접 만지는 건 02차시부터. 미리 알려 둘 것.

---

## 1. 게임 코드는 "반응"으로 가득하다 (10분)

**핵심 메시지**

- 게임 로직 대부분은 "무언가 일어나면 → 무언가 한다"(체력 바뀌면→체력 바 갱신, 버튼 눌리면→스킬, 적 죽으면→점수·사운드·퀘스트).
- 고전적 두 방법 대비: **폴링(pull)** vs **이벤트(push)**. 이벤트가 깔끔하나 규모 커지면 두 한계가 걸림.

**판서**

```
반응 = 무언가 일어나면 → 무언가 한다
한계 ① 구독 해제 누락 (-= 잊기 쉬움)
한계 ② 이벤트 하나로 표현 안 됨 (더블클릭·연타·검색)
```

**보여줄 코드** — 폴링 vs 이벤트 한 쌍(교안 1절):

```csharp
// 폴링: 매 프레임 "바뀌었나?"를 묻는다 — 바뀌지 않은 99%의 프레임에도
private void Update()
{
    if (m_Hp != m_LastHp) { m_HpText.text = $"HP : {m_Hp}"; m_LastHp = m_Hp; }
}

// 이벤트: 바뀐 쪽이 알려 준다
public event Action<int> HpChanged;
m_Player.HpChanged += hp => m_HpText.text = $"HP : {hp}";
```

**두 한계**

- **구독 해제 누락** — `+=`는 `-=`로 손수 해제. `OnDestroy`에서 빠뜨리면 메모리 누수·`MissingReferenceException`. 잊기 쉬운 수동 작업.
- **합성 불가** — "더블클릭", "0.5초 안에 세 번(연타)", "타이핑 멈춘 뒤(검색)"는 `event` 하나로 안 됨. 결국 `Update`·코루틴에 플래그·타이머를 직접 쌓음.

**보여줄 코드** — 명령형 더블클릭(읽기만, 부담을 눈으로):

```csharp
// 명령형: 더블클릭 하나에 코루틴 + 상태 플래그 + 타이머 + 조건문이 필요하다
private IEnumerator DetectDoubleClick()
{
    m_Running = true;
    int clickCount = 1;
    float timeLeft = 0.3f;
    while (timeLeft > 0f && clickCount < 2)
    {
        yield return null;
        if (Mouse.current.leftButton.wasPressedThisFrame) clickCount++;
        timeLeft -= Time.deltaTime;
    }
    if (clickCount == 2) Debug.Log("더블 클릭");
    m_Running = false;
}
```

- 핵심 질문으로 연결: 이벤트를 "시간 위 데이터 스트림"으로 보고, 구독 수명을 표준 관리하며, 연산자로 합성. 그 답이 리액티브.

---

## 2. 토대: 옵저버 패턴 (8분)

> 옵저버 이미 배운 반이면 3분으로 압축. 모르는 반엔 이 절이 미니 옵저버 수업.

**핵심 메시지**

- **주체(Subject)** — 사건을 일으키는 쪽. 관찰자 명단을 들고 사건 시 전원 알림.
- **관찰자(Observer)** — 알림 받아 반응. 주체에 자신을 **등록(구독)**.
- C#은 이 패턴을 `event`로 언어에 내장.

**보여줄 코드** — `event`가 곧 옵저버임:

```csharp
public class ScoreBoard
{
    public event Action<int> ScoreChanged;   // 관찰자 명단
    public void AddScore(int amount) { m_Score += amount; ScoreChanged?.Invoke(m_Score); }
}

scoreBoard.ScoreChanged += OnScoreChanged;   // 구독
scoreBoard.ScoreChanged -= OnScoreChanged;   // 해제 (잊기 쉬움!)
```

**판서** — "주는 것"과 "못 주는 것":

| 옵저버가 주는 것                   | 옵저버가 못 주는 것                         |
| ---------------------------------- | ------------------------------------------- |
| 일대다 알림                        | 해제의 안전망 (수동 `-=`)                   |
| 느슨한 결합 (주체는 관찰자를 모름) | 사건의 합성 (여러 사건·시간 조건 조합 수단) |

---

## 3. 리액티브 = 옵저버 패턴의 확장 (10분)

**핵심 메시지**

- ReactiveX 공식 정의: "Rx는 옵저버 패턴을 **데이터/이벤트의 시퀀스**를 지원하도록 **확장**하고, 그 시퀀스를 **선언적으로 합성하는 연산자**를 추가." → **대체가 아니라 일반화/확장**.

**판서** — 칠판에 크게:

```
Observable = 옵저버 패턴(주체-관찰자) + Operator(연산자 합성) + IDisposable(구독 수명)
```

**대응 화살표 4개** (아는 옵저버에 이름표 바꿔 달기):

| 옵저버 패턴 | 리액티브    | 비고                |
| ----------- | ----------- | ------------------- |
| 주체        | Observable  | 관찰당할 수 있는 것 |
| 관찰자      | Observer    | Subscribe로 반응    |
| `+=`        | `Subscribe` | 구독                |
| `-=`        | `Dispose`   | 표준화된 해제       |

**옵저버 위에 더해지는 3가지**

1. **연산자 합성("LINQ to events")** — 이벤트 스트림에 `Where`/`Select`/`Merge`를 직접 적용·조합. **단순 옵저버 대비 핵심 차이.**
2. **에러·완료 채널** — 값(`OnNext`)뿐 아니라 에러·완료 두 시맨틱 추가.
3. **표준화된 해제(`IDisposable`)** — 구독이 곧 `IDisposable`. `-=` 누락을 구조적으로 다룸.

> ⚠️ 예상 함정 질문 "event는 버리나?" → **확장이지 대체 아님**, 즉답. 안 쓸 때 기준은 5차시.

---

## 4. 스트림으로 생각하기 — 마블 다이어그램 (8분)

> 이 절은 **칠판 중심**. 슬라이드 읽지 말고 직접 그릴 것.

**핵심 메시지**

- 발상 전환: **이벤트는 한 번의 사건이 아니라 시간 축 위에 늘어선 데이터 흐름(스트림).** 유니티 **타임라인** 비유가 직관적.
- 마블 다이어그램 = 가로선(시간) + 구슬(`o`, 발행 값) + 연산자 상자.

**판서 ①** — 클릭 스트림을 `Where` 상자에 통과:

```
in:   --1--2--3--4--5-->
      +--------------------+
      | Where(x => x%2==0) |
      +--------------------+
out:  -----2-----4----->
```

**판서 ②** — 학생이 `Select(x => x*10)` 출력 직접 그리기:

```
in: --1----2----3-->   →   out: --10---20---30-->
```

**판서 ③** — 스트림의 세 가지 끝(완료 `|` / 에러 `X` / 무한 `-->`):

```
complete: --o--o--o--|     (예: Range(1,3))
error:    --o--o--X        (예: 네트워크 실패)
infinite: --o--o--o--o-->  (예: 매 프레임·클릭)
```

> 💡 연산자 문서가 전부 이 그림. **읽는 법**만 익히면 처음 보는 연산자도 그림으로 이해. 그리기=이해 확인용, 읽기=필수.

---

## 5. push vs pull — 무엇이 뒤집혔나 (5분)

**핵심 메시지**

- `IEnumerable`과 `Observable`은 데이터 전달 방향만 반대인 거울 대응 관계. 같은 연산자 어휘가 "메모리의 컬렉션"(pull) 대신 "시간을 두고 도착하는 이벤트"(push)에 적용 → Rx = **"LINQ to events"**.

**판서** — pull/push 대비:

| 구분        | `IEnumerable<T>` (pull)         | `Observable<T>` (push)               |
| ----------- | ------------------------------- | ------------------------------------ |
| 데이터 흐름 | 소비자가 **끌어옴**(`MoveNext`) | 생산자가 **밀어 넣음**(`OnNext`)     |
| 주도권      | 소비자 ("다음 값 줘")           | 생산자 ("값 나왔다, 받아라")         |
| 시간        | 무관 — 원할 때 꺼냄             | 본질 — **언제** 오는지가 정보의 일부 |
| 대표 도구   | LINQ                            | Rx 연산자 (같은 이름!)               |

- LINQ 아는 반이면 "리액티브의 절반은 이미 끝났다". 게임에서 push 자연스러운 것(입력·UI·체력/점수·충돌·네트워크) 한 줄 언급.

---

## 6~7. 선언적 합성 미리 보기 + Rx 계보 (5분)

**선언적 합성 — 읽기만**

1장 명령형 더블클릭의 R3 버전(문법은 3차시):

```csharp
// 선언형: "클릭이 잠잠해질 때까지 모아서, 2개 이상이면 더블클릭"
clickStream
    .Chunk(clickStream.Debounce(TimeSpan.FromMilliseconds(250)))
    .Where(clicks => clicks.Length >= 2)
    .Subscribe(_ => Debug.Log("더블 클릭"));
```

- 비교 포인트는 줄 수 아님. **명령형="어떻게(how)"**(플래그·타이머·분기), **선언형="무엇을(what)"**(의도가 그대로 코드). 상태 관리 실수 여지도 사라짐.
- "의도가 그대로 코드"만 전달, 문법은 3차시. 공짜 아님(연산자 어휘 학습·중간 디버깅 난이도)도 한 줄.

**Rx 계보** — 가드레일 준수(날짜·버전·점유율 단정 금지):

```
Rx.NET(1세대) → UniRx(2세대, 아카이브) → R3(3세대, 이 강의)
```

- UniRx는 **아카이브(읽기 전용)** 상태, README가 "R3를 대신 쓰라" 안내.
- R3는 같은 저자 계열(neuecc, Cysharp) 정식 후속, 범용 .NET 라이브러리(Godot·Avalonia·WPF 등). 유니티 통합은 `R3.Unity`, **Unity 2021.3 이상** 지원.

---

# 🎓 이론 마무리 (2분)

| 개념            | 한 줄 요약                                                       |
| --------------- | ---------------------------------------------------------------- | ------------- |
| 반응으로 가득   | 게임 코드는 "일어나면→한다" — 폴링·수동 event엔 한계(해제·합성)  |
| 옵저버의 확장   | `Observable = 옵저버 + Operator + IDisposable`                   |
| 스트림으로 보기 | 이벤트는 시간 축 위의 데이터 흐름 — 마블(`                       | `/`X`)로 읽음 |
| push vs pull    | `IEnumerable`(pull) ↔ `Observable`(push), Rx = "LINQ to events" |
| 계보            | Rx.NET → UniRx(아카이브) → R3(이 강의)                           |

- 마무리 활동: 체크리스트 6항목 중 2개 무작위로 골라 옆 사람에게 설명시키기.

> 📌 **다음 차시 예고** — 02차시부터 교안 + **데모 씬(`ReactiveBasics` §1 생성)**으로 R3 코드를 직접 다룸. 오늘 "읽기만" 한 Observable·Subscribe를 다음 시간에 손으로.

---

## 💡 강사 팁 · 예상 Q&A

**진행 팁**

- 시간 늘어지면 6~7절 압축. 4절(마블)이 핵심이니 여기서 질문 받아 이해 확인 후 진행.
- "핵심은 연산자 합성"을 3절에서 분명히 짚지 않으면 학생이 R3를 "그냥 예쁜 event"로 오해.

**예상 Q&A**

> ❓ **함수형 프로그래밍이랑 같은 건가?**
> 친척이지만 다름. 리액티브는 "시간 위의 데이터 흐름"이 중심이고, 함수형의 도구(람다·불변·합성)를 많이 빌려 씀. "함수형 reactive programming(FRP)"이라는 학술 용어와 Rx는 엄밀히는 다르다는 논쟁도 있으나 수업에서는 안 들어가는 게 맞음.

> ❓ **이거 배우면 코루틴은 안 써도 되나?**
> 아님. 코루틴/async는 "한 줄기 절차"를 시간에 걸쳐 진행하는 데 강하고, 리액티브는 "반복 이벤트의 합성"에 강함. 5차시에서 둘을 잇는 법(await 통합)을 배움.

> ❓ **마블 다이어그램 꼭 그려야 하나?**
> 연산자 문서가 전부 이 그림이라, 읽을 줄 모르면 공식 문서를 못 봄. 그리는 건 이해 확인용이고, 읽는 게 필수.

> ❓ **bool 플래그를 두고 상태가 바뀔 때마다 UI를 갱신하던 건 R3로 어떻게 바뀌나?** (전환형)
> "현재값을 들고 있다가 바뀌면 자동 통지"하는 `ReactiveProperty`가 그 자리를 대체함(2차시에서 코드로). 지금은 "플래그 + 수동 갱신 호출"이 "값 보유 스트림 + 자동 구독"으로 바뀐다는 예고만 — 개념 차시이므로 가볍게. ('기존 방식 → R3' 한눈 매핑표는 [00. 강사 사전 학습 가이드](00.%20강사%20사전%20학습%20가이드.md))

---

## 이해도 퀴즈

1. "리액티브는 옵저버 패턴의 확장"을 `Observable = 옵저버 + Operator + IDisposable` 프레임으로 설명하시오.
2. C# `event` 기반 옵저버의 두 가지 고질병을 쓰시오.
3. `--1--2--3--|` 스트림에 `Where(x => x >= 2)`를 적용한 출력 마블을 그리시오.
4. push와 pull의 차이를 `IEnumerable`/`Observable`로 설명하시오.

> ✅ **해설**
>
> 1. 주체/관찰자 구조는 그대로 두고, 그 위에 연산자 합성(스트림 가공)과 표준 해제(구독=IDisposable)를 더한 것.
> 2. ① `-=` 누락 시 누수·`MissingReferenceException` ② 시간·횟수가 얽힌 사건(더블클릭 등)을 합성할 표준 수단이 없음.
> 3. `-----2--3--|` (1만 걸러지고 완료는 유지).
> 4. `IEnumerable`은 소비자가 `MoveNext`로 끌어오고(pull), `Observable`은 생산자가 `OnNext`로 밀어 넣음(push). 값이 "언제" 오는지가 push에서는 정보의 일부.

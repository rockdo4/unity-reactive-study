# 03. 연산자 따라하기 — 강사용

> 진행 메모(라이브 코딩 따라하기, ~25분). 학생 화면=노션 교안 03 공유. 빈 메서드를 한 줄씩 함께 채움. 데모마다 ① 빈 메서드 한 줄씩 작성 → ② 실행·확인 → ③ 마블로 옮기기/변형·관찰.
>
> - **선행 수업**: [03. 연산자 이론 - 강사용](03.%20연산자%20이론%20-%20강사용.md)
> - **학생 교안**: [03. 연산자 - 스트림을 다듬고 합성하기](../교안/03.%20연산자%20-%20스트림을%20다듬고%20합성하기.md)
> - **씬(강사 확인·시연용)**: `Assets/Scenes/ReactiveBasics`(완성본)의 §2 연산자 섹션 · **모범 코드(강사용 완성본·도달점)**: `Assets/Scripts/ReactiveBasics/OperatorsDemo.cs`
> - **학생 배포본(스켈레톤)**: 패키지 `R3Lecture-Student-1-Basics`(클래스명 `MyOperatorsDemo`). R3 코드를 비운 스켈레톤(주석 없음). `[SerializeField]` 필드·`SetText` 등 보조 헬퍼·생명주기 제공, R3 스트림 세 Setup 본문만 빔: `SetupDoubleClick`·`SetupSearchDebounce`·`SetupTapAndCooldown`. `Start()`의 세 Setup 호출은 제공됨.
> - 📂 도구: [씬 생성·패키지 도구 가이드](../데모/씬%20생성·패키지%20도구%20가이드.md)

## 📚 수업 개요

- **시간**: 약 25분 (준비 3분 + 4개 데모 + 마무리)
- **학습 목표**: 이론에서 배운 연산자(필터·변환·시간 3총사·결합)로 빈 메서드를 직접 채워 각 파이프라인의 발화를 눈으로 확인하고 마블로 옮김

## ✅ 수업 전 체크리스트

- [ ] 모범 코드(`OperatorsDemo.cs`)로 도달점 숙지, 가능하면 보지 않고 세 Setup 한 번 직접 채워 볼 것 — 막히는 지점 메모.
- [ ] `ReactiveBasics` 씬(완성본) Play → §2 연산자 4개 데모(더블클릭·검색어·3연타·쿨다운) 도달점 미리 시연·확인
- [ ] 학생 스켈레톤(`MyOperatorsDemo`) 열어 세 Setup 빈 본문 + 제공된 `[SerializeField]`·`SetText`·`Start()` Setup 호출 확인
- [ ] 강사 PC: 씬 정상 Play + Console 창 보이게 레이아웃 정리(더블클릭·연타·쿨다운 로그가 프레임 번호와 함께 찍힘)
- [ ] Game 뷰 해상도 16:9(패널 기준 1920×1080)
- [ ] UniRx `Throttle` 고의 실패 시연 준비 — 학생이 검색해 올 UniRx 한 줄 머리에 넣어 둘 것
- [ ] 씬 없으면 강사는 `Tools > Reactive Study > Create All Scenes`(또는 `01. R3 기초 통합 씬 생성`)로 생성(비커밋), 학생은 패키지 재임포트

## 준비 (3분)

1. **완성본 시연**(강사): `Assets/Scenes/ReactiveBasics` Play → §2 4개 데모 1분 시연. 오늘 빈 메서드 세 개를 함께 채워 이걸 만든다는 점.
2. **학생 스켈레톤 확인**: 학생은 자기 씬 Play → 4개 데모 무반응 확인(Setup 비어 있으므로).
3. 화면 구성 — 더블클릭 영역 / 검색어 입력 / 탭 버튼(연타·쿨다운) 세 묶음.
4. Console 창 같이 띄움 — 더블클릭·3연타·쿨다운은 텍스트와 함께 프레임 번호가 찍혀 발화 시점이 보임.
5. **스켈레톤 구조**: 클래스명 `My` 접두사(`MyOperatorsDemo`). 채울 곳은 세 Setup뿐, `[SerializeField]`·`SetText`·`Start()` Setup 호출은 제공됨.

> ⚠️ **함정**: 완성본 씬과 학생 스켈레톤은 다른 클래스(`OperatorsDemo` vs `MyOperatorsDemo`). 학생이 시연만 보고 된다고 착각하면, 자기 스켈레톤 빈 메서드에서 작업 중인지 확인.

> 💡 진행 한눈표 — 더블클릭: 화면 빠르게 두 번 / 검색: 타이핑 후 멈춤 / 3연타: 탭 빠르게 3번 / 쿨다운: 탭 연타.

---

## 데모 1: 더블클릭 — Chunk + Debounce 경계 (7분)

> 🔧 **오늘 쓸 부품**: 03차시 `EveryUpdate`·`Where`·`Share`·`Chunk`·`Debounce`.

빈 `SetupDoubleClick()` 함께 채움. `EveryUpdate → Where → Share`로 클릭 스트림, `Chunk(Debounce 경계)`로 묶음:

```csharp
private void SetupDoubleClick()
{
    // 매 프레임 스트림에서 "왼쪽 버튼이 눌린 프레임"만 거른다 (Input System 사용)
    // Share(): 아래에서 본체와 Debounce 경계 두 곳이 같은 스트림을 쓰므로 구독을 하나로 공유한다
    var clickStream = Observable
        .EveryUpdate()
        .Where(_ => Mouse.current != null && Mouse.current.leftButton.wasPressedThisFrame)
        .Share();

    // 클릭이 250ms 동안 잠잠해지면(Debounce) 그때까지 모인(Chunk) 클릭을 한 묶음으로.
    // 묶음에 2개 이상 들어 있으면 더블클릭이다.
    clickStream
        .Chunk(clickStream.Debounce(TimeSpan.FromMilliseconds(250)))
        .Where(clicks => clicks.Length >= 2)
        .Subscribe(clicks =>
            SetText(m_DoubleClickText, $"더블클릭! ({clicks.Length}회 묶음, 프레임 {Time.frameCount})")
        )
        .AddTo(this);
}
```

- 한 문장 읽기: "클릭이 250ms 잠잠해질 때까지 모아서, 2개 이상이면 더블클릭." 1차시 명령형 더블클릭(코루틴+플래그+타이머)과 대비 — 의도가 그대로 코드.

> ⚠️ **함정**: `Share()` 질문 나오면 — clickStream을 본체와 Debounce 경계 두 곳이 써서 구독을 하나로 공유하는 것까지만. 핫/콜드 깊은 설명은 이론 3-5 수준으로 제한.

**실행·확인** (작성 후 Play):

1. 화면 **천천히 두 번** 클릭 → 반응 없음.
2. 화면 **빠르게 두 번** 클릭 → "더블클릭!" + 묶음 개수·프레임 번호. Debounce 경계(250ms) 체감.

**이론 연결**: 3-3(Chunk·Debounce), 3-5(Share).

## 데모 2: 검색어 입력 — Debounce + DistinctUntilChanged (5분)

> 🔧 **오늘 쓸 부품**: 03차시 `Debounce`·`DistinctUntilChanged`.

빈 `SetupSearchDebounce()` 함께 채움 — 자동완성·설정 자동 저장에서 흔한 조합:

```csharp
private void SetupSearchDebounce()
{
    // 타이핑이 500ms 멈췄을 때만, 그리고 직전 검색어와 다를 때만 "검색"을 실행한다.
    // 자동완성·설정 자동 저장에서 흔히 쓰는 조합 (Debounce + DistinctUntilChanged)
    m_SearchInput
        .onValueChanged.AsObservable()
        .Debounce(TimeSpan.FromMilliseconds(500))
        .DistinctUntilChanged()
        .Subscribe(query =>
            SetText(
                m_SearchResultText,
                string.IsNullOrWhiteSpace(query) ? "검색 대기 중..." : $"검색 실행: \"{query}\""
            )
        )
        .AddTo(this);
}
```

- Debounce = "입력이 끝났을 때 한 번", DistinctUntilChanged = "직전과 같으면 무시". 두 역할 또렷이 분리.

**실행·확인** (작성 후 Play):

1. 타이핑 **중에는 침묵** → 멈추면 0.5초 뒤 "검색 실행".
2. 같은 검색어 다시 입력 → **무시**(DistinctUntilChanged).

> 📌 이 `Debounce` 한 줄을 학생이 방금 자기 스켈레톤에 작성함. 뒤 고의 실패 시연에서 바로 이 줄을 UniRx식 `Throttle`로 바꿔 이름 매핑 각인.

**이론 연결**: 3-2(DistinctUntilChanged), 3-3(Debounce).

## 데모 3·4: 3연타와 쿨다운 — 한 버튼의 두 정책 (7분)

> 🔧 **오늘 쓸 부품**: 03차시 `Share`·`Chunk`·`ThrottleFirst`.

빈 `SetupTapAndCooldown()` 함께 채움 — 같은 `taps` 스트림에 두 파이프라인이 독립적으로 붙음. 마블 먼저, 코드 나중. 칠판에 세 줄 그린 뒤 코드:

```
taps:         --t-t-t--------t-->
ThrottleFirst:--t------------t-->   (먼저 한 번, 구간 무시)
Chunk(500,3): --[t t t]---------->   (500ms 안에 3개면 묶음 발화)
```

```csharp
private void SetupTapAndCooldown()
{
    var taps = m_TapButton.OnClickAsObservable().Share();

    // 500ms 안에 3번 누르면 연타 (Chunk: 시간 또는 개수 경계로 묶기)
    taps.Chunk(TimeSpan.FromMilliseconds(500), 3)
        .Where(xs => xs.Length >= 3)
        .Subscribe(_ => SetText(m_TapText, $"3연타! (프레임 {Time.frameCount})"))
        .AddTo(this);

    // 첫 클릭만 통과시키고 2초간 무시 (ThrottleFirst: 스킬 쿨다운)
    taps.ThrottleFirst(TimeSpan.FromSeconds(2))
        .Subscribe(_ => SetText(m_CooldownText, $"발동! 2초 쿨다운 (프레임 {Time.frameCount})"))
        .AddTo(this);
}
```

- 강조점: 스트림 하나에 정책 여러 개. `Share()`가 한 버튼 클릭을 두 파이프라인이 나눠 받게 함.

**실행·확인** (작성 후 Play):

1. 탭 버튼 **빠르게 3번**(500ms 안에) → "3연타!". 느리게 누르면 묶음이 3개를 못 채워 무반응.
2. 탭 버튼 **연타** → "발동!"은 2초에 한 번만. (같은 버튼인데 위 3연타와 독립.)

> 📌 회수 포인트: `Share()`를 빼면 동작은 하나 버튼 클릭 구독이 두 벌. "두 곳 이상에서 쓰면 Share"(3차시).

**이론 연결**: 3-3(Chunk·ThrottleFirst), 3-5(Share).

---

## 🔻 고의 실패 시연 — UniRx `Throttle` (3분)

학생이 검색해 오는 코드는 UniRx 기준일 확률 높음. 데모 2에서 작성한 `Debounce` 줄을 UniRx식 `Throttle`로 바꿔 컴파일 에러를 Console에서 보여 주고 매핑표로 복구.

1. 데모 2의 `Debounce`를 UniRx식 `Throttle`로 바꿔 입력 → 저장.
    ```csharp
    .Throttle(TimeSpan.FromMilliseconds(500))   // ← UniRx 이름. R3엔 없음
    ```
2. Console에 컴파일 에러(`Throttle` 정의 없음). Unity MCP `Unity_GetConsoleLogs`로도 확인 가능.
3. 이론 3-5 매핑표로 복구 — 다시 `Debounce`로 되돌림: `Throttle → Debounce`(최대 함정 — R3엔 Throttle 없음), `Buffer → Chunk`(`.Count → .Length`).
4. 요점: 설명보다 컴파일 에러 한 번이 매핑을 각인.

> 🔧 학생 코드 꼬이면 `R3Lecture-Student-1-Basics` 재임포트로 스켈레톤 초기화(작성 중 코드 백업 안내). 강사 데모 꼬이면 `git checkout -- Assets/Scripts/`로 원복 후 다시 Play.

---

# 🎓 마무리

## 4개 데모 ↔ 연산자 요약

| 데모          | 사용 연산자                                      | 확인한 것                                |
| ------------- | ------------------------------------------------ | ---------------------------------------- |
| 화면 더블클릭 | `EveryUpdate`+`Where`+`Share`+`Chunk`+`Debounce` | 빠르게 두 번 클릭할 때만 반응            |
| 검색어 입력   | `Debounce`+`DistinctUntilChanged`                | 타이핑 멈춘 뒤 0.5초, 같은 검색어는 무시 |
| 3연타         | `Chunk(500ms, 3)`                                | 느리게 누르면 묶음이 3개를 못 채움       |
| 쿨다운        | `ThrottleFirst(2s)`                              | 연타해도 2초에 한 번만 발동              |

> 💡 마무리: 학생이 채운 세 Setup(`MyOperatorsDemo`) 같이 띄우고 각 파이프라인을 마블로 손그림(막힌 학생은 `OperatorsDemo.cs`와 대조). 시간 3총사·결합 2총사 복기, "한 스트림을 두 곳 이상에서 쓰면? → Share 의심"으로 닫음.

## 마무리 Q&A 예상 질문

> ❓ **체인이 길어지면 디버깅은?**
> ① 중간 스트림을 지역 변수로 쪼개 이름 붙임 ② `Do`(R3 사이드 이펙트 연산자)나 중간 `Subscribe`+로그로 단계별 값 찍음 ③ Tracker로 구독 확인(4차시). "7~8개 이상 이어지면 설계 의심"이 교안 권고.

> ❓ **UniRx 코드의 `Buffer`·`Throttle`을 R3로 옮길 때?** > `Buffer → Chunk`(결과 `.Count → .Length`), `Throttle → Debounce`(R3에 Throttle 없음). 고의 실패 시연 그대로.

## 이해도 퀴즈

1. 같은 연타 입력에 대해 Debounce와 ThrottleFirst가 각각 언제 발화하는지 마블로 그리시오.
2. 더블클릭 파이프라인 `clicks.Chunk(clicks.Debounce(250ms)).Where(xs => xs.Length >= 2)`를 한국어 한 문장으로 읽으시오.
3. "HP > 0이고 탄약 > 0일 때만 발사 버튼 활성화"를 어떤 연산자로 만드는지 코드 골격을 쓰시오.
4. UniRx 코드의 `Buffer`·`Throttle`을 R3로 옮길 때 각각 무엇으로 바꾸는가?

> ✅ **해설**
>
> 1. 입력 `--a-b-c--------`에 대해 Debounce는 잠잠해진 뒤 `c` 한 번, ThrottleFirst는 시작 즉시 `a` 한 번.
> 2. "클릭이 250ms 잠잠해질 때까지 모아서, 2개 이상이면 더블클릭."
> 3. `hp.CombineLatest(ammo, (h, a) => h > 0 && a > 0).DistinctUntilChanged().Subscribe(can => button.interactable = can)`
> 4. `Buffer→Chunk`(결과 `.Count`→`.Length`), `Throttle→Debounce`(R3에 Throttle 없음).

---

## 💡 강사 팁 · 트러블슈팅

- **단계마다 중간 Play로 확인** 후 다음 데모. 세 메서드를 한 번에 다 치고 Play하면 어디서 깨졌는지 못 찾음. 코드 변경 후 `Unity_GetConsoleLogs`로 에러 확인.
- **채웠는데도 무반응**: 먼저 의심 — 해당 Setup이 아직 빈 채, 코드를 다른 메서드에 넣음, `.AddTo(this)`/`.Subscribe(...)` 누락. `Start()`의 세 Setup 호출 줄은 스켈레톤에 이미 있음.
- **흔한 오류**: Inspector 연결 누락(버튼·텍스트 무반응 — 가장 흔함, 스켈레톤 씬엔 연결돼 있으나 학생이 오브젝트를 새로 만들었다면 직접 연결), 한글 깨짐(`R3Lecture-Student-1-Basics`를 먼저 임포트했는지 — 폰트 포함), EventSystem 부재(클릭 무시).
- 강사 씬 꼬이면 `Tools > Reactive Study > Create All Scenes`로 재생성(비커밋). 학생 코드 꼬이면 `R3Lecture-Student-1-Basics` 재임포트로 초기화(작성 중 코드 백업 안내).
- 더블클릭 안 잡히면 더 빠르게 두 번 — 250ms 안에 두 클릭. 천천히 누르면 의도대로 무반응.
- 막힌 학생에겐 `OperatorsDemo.cs` 해당 부분 화면 공유하되 복붙으로 건너뛰지 말 것(타이핑이 학습). 에러도 교보재 — 그 자리에서 함께 디버깅.
